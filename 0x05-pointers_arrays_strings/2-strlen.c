#include "main.h"
#include <stdio.h>
/**
 * _strlen - calculates the size of a string
 *
 * @s: string that code is checking
 * Return: always 0.
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	return (i);
}
